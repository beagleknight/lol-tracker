{
  "buildCommand": "npx tsx scripts/migrate.ts && npx tsx scripts/seed-demo.ts && if [ \"$VERCEL_ENV\" = \"preview\" ]; then npx tsx scripts/seed.ts --force-remote; fi && next build --webpack",
  "regions": ["dub1"]
}
